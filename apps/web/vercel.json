{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd /vercel/path0 && pnpm install --frozen-lockfile && cd /vercel/path0/apps/web && SKIP_ENV_VALIDATION=1 pnpm build",
  "installCommand": "cd /vercel/path0 && pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next"
}
