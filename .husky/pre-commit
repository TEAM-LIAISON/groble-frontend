#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 변경된 파일에 대해서만 빠른 검사 실행
echo "🔍 Pre-commit: 변경된 파일 검사 중..."

# lint-staged로 변경된 파일만 체크
pnpm exec lint-staged

# 성공 메시지
echo "✅ Pre-commit 검사 완료!"